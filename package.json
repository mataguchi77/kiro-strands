{
  "name": "bedrock-qa-agent",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local": "sam local invoke BedrockQAFunction -e test-event.json"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent-runtime": "^3.700.0",
    "@aws-sdk/client-bedrock-runtime": "^3.700.0"
  }
}
